name: Test Sem

on:
  workflow_dispatch:
    branches: [main,dock]

jobs:
  lint:
    name: "Смотрим линт. Все ли хорошо"
    runs-on: ubuntu-latest
    steps:
      # Шаг 1:
      - name: flake8,pylint,black,mypy
        run: echo "смотрим линт"
  test:
    name: "Скачаем зависимости и Запуск тестов"
    runs-on: ubuntu-latest
    needs: lint
    steps:
      # Шаг 1: Скачивание кода моего репозитория
      - name: 📥 Скачивание кода моего
        uses: actions/checkout@v4
      # Шаг 2: Установка версии Python

      - name: 🐍 Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Установим зависимости (requirements.txt)
        run: |
           python -m pip install --upgrade pip
           pip install -r requirements.txt
      - name: смотрим что в папках
        run: |
          pwd
          ls -la
          cd tests
          ls -la
      # 🔍 нахождения файла
      - name:  Find test file
        run: |
          pwd
          find . -name "*wallett*.py" -o -name "*test*.py"
      - name: 🧪 Run tests
        run: pytest tests/test_wallett.py -v
        # pytest tests/ -v --html=results/report.html --self-contained-html
        env:
            PYTHONPATH: ${{ github.workspace }}  # 👈 Добавляем корень репозитория в путь
#      # 👇 СОХРАНЯЕМ РЕЗУЛЬТАТ   артефакты
#      - name: 📦 Upload test report
#        uses: actions/upload-artifact@v4
#        if: always()  # 👈 Загрузить даже если тесты упали!
#        with:
#          name: pytest-report
#          path: results/
  build:
    name: "Создаем docker image"
    needs: [test,lint]
    runs-on: ubuntu-latest

    steps:
      # Шаг 1: Скачивание кода репозитория
      - name: Checkout кода
        uses: actions/checkout@v4
      # Шаг 2: Авторизация в Docker Hub
      - name: Login в Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN_GIT }}
      # Шаг 3: Сборка и отправка образа (Build & Push)
      - name: Build и Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            sem87sem/wallet:latest
            sem87sem/wallet:3.7
            sem87sem/wallet:${{ github.sha }}
  deploy:
    name: "выкладываем на сервер"
    runs-on: ubuntu-latest
    needs: build
    steps:
      # Шаг 1: создаем Докер
      - name: заливаем на сервер
        run: echo "заливаем на сервер"



#strategy:
#  matrix:
#    os: [ubuntu-latest]   # windows-latest,macos-latest
#runs-on: ${{  matrix.os }}